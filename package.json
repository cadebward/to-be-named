{
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna clean",
    "init": "lerna bootstrap",
    "start": "cd packages/cadal-server && npm start",
    "watch": "cd packages/cadal-client && npm run watch"
  },
  "devDependencies": {
    "lerna": "2.0.0-beta.30"
  }
}
